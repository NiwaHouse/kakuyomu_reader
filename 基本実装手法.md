# 基本実装手法 - カクヨム・Web小説リーダー

## 1. 依存ライブラリと推奨構成
本プロジェクトで採用する主要ライブラリおよびバージョン定義の仕様例です。

- **Kotlin**: 1.9.x / 2.0.x
- **Android Gradle Plugin (AGP)**: 8.x
- **Jetpack Compose**: Compose BOM (2024.x) + Material3
- **AndroidX WebKit**: `androidx.webkit:webkit:1.11.0` (WebView最新機能)
- **AndroidX Media3 / MediaSession**: `androidx.media3:media3-session:1.3.1`, `androidx.media3:media3-ui:1.3.1` (通知バー連携)
- **Kotlin Coroutines**: `org.jetbrains.kotlinx:kotlinx-coroutines-android:1.8.0`
- **AndroidX Lifecycle & ViewModel**: `androidx.lifecycle:lifecycle-viewmodel-compose:2.8.0`, `androidx.lifecycle:lifecycle-runtime-compose:2.8.0`

---

## 2. 実装のベストプラクティスと技術仕様

### 2.1. ルビ除外とJavaScript DOM抽出
- **課題**: 小説サイトでは `<ruby>漢字<rt>かんじ</rt></ruby>` とマークアップされているため、単に `innerText` を取得すると「漢字かんじ」と二重に読まれる。
- **対策**: タップされた要素の `cloneNode(true)` を作成し、クローン側の `<rt>` 要素をすべて `remove()` してから `innerText` を取得する。
- **実装例**:
  ```javascript
  let clone = target.cloneNode(true);
  clone.querySelectorAll('rt').forEach(rt => rt.remove());
  let cleanText = clone.innerText || clone.textContent;
  ```

### 2.2. TTSの長文チャンキング（分割再生）
- **課題**: Android標準TTS（特に端末内蔵エンジン）は、極端に長い文章（約4000文字超や一部機種のバッファ上限）を一括で `speak()` すると、途中で音声が途切れるかクラッシュする。
- **対策**: `TextChunker` を介して、句読点（`。`、`！`、`？`、改行）を基準に約100〜200文字前後のチャンクに分割し、1つ目を `QUEUE_FLUSH`、2つ目以降を `QUEUE_ADD` で順次キューイングするか、1チャンク終了ごとに次チャンクを再生する。

### 2.3. バックグラウンド再生（Foreground Service & Media3）
- **課題**: Android 8.0以降（特にAndroid 14以降）のバックグラウンド制限により、Activityが停止するとTTSが破棄・停止される。
- **対策**: 
  - `FOREGROUND_SERVICE` および `FOREGROUND_SERVICE_MEDIA_PLAYBACK` 権限を宣言。
  - `NovelPlaybackService` をフォアグラウンドサービスとして起動し、Media3 `MediaSession` または `NotificationCompat.MediaStyle` を用いて永続通知を表示。
  - Activity破棄時もServiceがTTSのライフサイクルを維持し、ロックスクリーンや通知パネルから停止/再生が可能。

### 2.4. 自動連続読み上げ（DOMチェーン連携）
- **課題**: 1つの段落を読み終えた後、ユーザーが操作しなくても次の段落へ進む必要がある。
- **対策**:
  - TTSの `UtteranceProgressListener.onDone` を検知。
  - AndroidからWebViewに対し `javascript:window.NovelReaderBridge.readNextParagraph('current_id')` を実行。
  - JS側で `nextElementSibling` または次段落セレクタを探索し、存在すれば `AndroidBridge.onNextParagraphFound(id, text)` をコールバック。

---

## 3. 注意点・アンチパターン（エラー回避ルール）

1. **`JavascriptInterface` のメインスレッド注意点**:
   - JSから `JavascriptInterface` に送られるコールバックはバックグラウンドスレッドで実行される。
   - UIの変更やStateFlowの更新は、必要に応じて `Dispatchers.Main` や `viewModelScope` にディスパッチすること。

2. **TTSエンジンの非同期初期化**:
   - `TextToSpeech` インスタンス生成直後に `speak()` を呼ぶと失敗する。
   - 必ず `onInit(status)` で `TextToSpeech.SUCCESS` を確認し、`isTtsReady = true` になってから再生を許可すること。

3. **WebViewのメモリリーク防止**:
   - Composeで `AndroidView` を使用する場合、`onRelease` や `DisposableEffect` で `webView.destroy()` を適切に呼び出す。

---

## 4. ビルド・検証結果
- **Gradleビルド**: `./gradlew assembleDebug` により、エラーやワーニングなくコンパイル・ビルドが成功することを確認済み。
- **ユニットテスト**: `RubyFilterTest` および `TextChunkerTest` を作成し、`./gradlew testDebugUnitTest` で全テストが PASS することを確認済み。
