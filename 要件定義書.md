# 要件定義書 - スピークブラウザ (Speak Browser) - Web小説・Webページ読み上げブラウザ

## 1. プロジェクト概要
カクヨムをはじめとするWeb小説サイトやWebページのコンテンツを閲覧しながら、ユーザーがタップした段落からAndroid標準のTextToSpeech（TTS）で滑らかに音声読み上げを行うAndroidネイティブブラウザアプリケーション（スピークブラウザ）。
アプリアイコンは開いた本とスピーカーの音波を融合した洗練されたモノクロデザインを採用。

## 2. 動作環境・仕様フレームワーク
- **対象プラットフォーム**: Android OS (Min SDK: 26 / Target SDK: 35)
- **開発言語**: Kotlin
- **UIフレームワーク**: Jetpack Compose (Material3)
- **非同期・状態管理**: Kotlin Coroutines, StateFlow
- **音声・バックグラウンド再生**: Android 標準 TextToSpeech (TTS), AndroidX Media3 (MediaSession), Foreground Service

## 3. 機能要件

### 3.1. Web小説閲覧・ブラウジング機能
- **WebView表示**: カクヨムや小説家になろう等のWeb小説ページおよびWebサイトを表示。
- **URLナビゲーション**: URL入力・進む・戻る・更新等のブラウザ基本機能。
- **ホームボタン**: ワンタップで Google (`https://www.google.com`) へ遷移するホーム機能。
- **お気に入り（ブックマーク）管理**: 画面上部の「☆」ボタンで現在ページをお気に入り登録。サイドドロワーで一覧表示・タイトル付き閲覧・削除可能。
- **閲覧履歴管理**: 閲覧したWebページをタイトル付きで自動記録。サイドドロワーから履歴閲覧・再アクセス・削除・全件消去が可能。
- **閲覧中サイト判定**: ロードされたURLに応じて最適なサイトパーサーを動的に選択・適用。

### 3.2. テキスト抽出・ルビフィルタリング機能
- **タイトル/親コンテナ一括抽出モード**: タップされた要素の親コンテナから本文段落を一括抽出し、アプリ側キューで画面OFF時も安定して連続再生。親を遡る階層数（1〜5段階）を設定可能。
- **段落順次クリックモード**: タップされた段落から1段落ずつ順次ハイライト・スクロールしながら読み進める。
- **ルビ（`<rt>`タグ）除外**: `<ruby>漢字<rt>かんじ</rt></ruby>` 等のフリガナタグを除去し、「漢字かんじ」と二重に読まれる現象を防止。
- **アクティブ段落ハイライト**: 現在読み上げている段落の背景色を変更し、視覚的に位置をフィードバック。

### 3.3. 音声読み上げ・連続再生機能
- **即時タップ再生**: タップした段落から即座にTTS読み上げを開始（`QUEUE_FLUSH`）。
- **自動連続読み上げ**: 1つの段落の読み上げ完了後、DOMツリー上の次の有効な段落（空行・挿絵をスキップ）を自動特定し、ハイライト・画面中央スクロール追従を行いながら連続して読み上げを継続。
- **次話への自動ナビゲーション**: 1つの話（エピソード）の全段落を読み終えた際、自動的に「次の話」のリンクを検出・WebViewで移動し、新しい話の第1段落から止まらずに自動で読み上げを再開。
- **再生スピードの継続維持**: ユーザーが変更・設定した再生スピード（`PlaybackSpeed`）はページ遷移を行ってもそのまま維持され、新しいページの読み上げにも継続適用。
- **最終話アナウンス＆自動停止**: 次の話が存在しない（作品の最終話）場合、「作品の末尾です」とTTS音声アナウンスを再生後、自動で再生を停止しサービス/通知を終了。
- **長文分割（Chunking）**: 長文を句読点（`。`、`！`、`？`、改行等）で適切に分割し、TTSの読み上げ途切れを防止。
- **再生速度調整**: 0.5倍速〜3.0倍速での動的な読み上げ速度変更。再生中に速度が変更された場合は、現在読み上げ中の段落を新しい速度で即座に読み直して再生を継続。
- **一時停止・再開・停止**: 読み上げの一時停止、途中からの再開、停止。

### 3.4. バックグラウンド再生・システム連携機能
- **BG再生ボタン（`moveTaskToBack` 連携）**: 画面上の「BG再生」ボタンタップ時、アプリをバックグラウンドに移動させると同時にフォアグラウンドサービスを常駐化。
- **Foreground Service**: アプリがバックグラウンドに移行した場合や画面ロック時でも途切れず読み上げを継続。
- **Media3 / MediaSession 連携**: ロック画面および通知ドロワーにメディアコントロール（再生/一時停止/次へ/前へ/停止）を表示し操作可能とする。
- **オーディオフォーカス制御**: 電話着信時や他メディア再生時の自動一時停止・復帰。

### 3.5. 辞書登録モード・ユーザー辞書置換機能
- **辞書登録モード**: コントロールバーの「📖 辞書」トグルで切り替え。ON時は音声再生を維持しつつスクロール追従および段落タップ再生を無効化し、通常のブラウザとして自由なテキスト選択を可能とする。
- **選択テキストからの辞書登録**: 文字選択中に「➕ 登録」ボタンを押すと選択テキストを「表記（単語）」に自動セットしたダイアログを表示し、「よみ」を入力して登録。文字未選択時は通知のみ表示。
- **ユーザー辞書自動置換**: ルビ除去と同時に、登録された辞書データ（長文優先）に基づいて本文中の該当テキストを「よみ」に自動置換してTTS読み上げ。
- **再生中テキストへの即時反映**: 辞書登録完了時、現在再生中の段落および抽出済みキューに対しても即座に新辞書置換を再適用し、再生を止めずに自動継続。
- **辞書管理**: サイドドロワーで登録済み辞書一覧の表示・個別に削除が可能。

### 3.6. サイト別パーサー拡張（Plugin機能）
- **共通パーサーインターフェース（`NovelSiteParser`）**: カクヨム、小説家になろう、汎用Web用パーサーをプラグインとして分離。
- **サイト固有ロジックの分離**: ドメイン判定、ルビ除外ルール、次話リンク抽出、本文要素セレクタ等を各パーサーでカプセル化。

## 4. 非機能要件
- **応答性**: タップから読み上げ開始まで遅延なくスムーズに反応すること。
- **安定性**: メモリリーク防止（TTSの適切なshutdown、WebViewのライフサイクル管理、Serviceバインド管理）。
- **コード品質・保守性**: 
  - 各ファイルは約200行以下を厳守。
  - クリーンアーキテクチャ × 機能単位（Feature-Based）フォルダ構成による高凝集・低結合。
  - 全関数・クラスに日本語ドキュメントコメントを付与。

## 5. フォルダ構成ルール（機能単位 / Feature-Based）
本プロジェクトは、技術レイヤーによるフラットな分割ではなく、機能単位（Feature-Based）でコードを配置し、各機能が自己完結する構成とします。

```text
app/src/main/java/com/example/kakuyomureader/
├── core/
│   ├── model/                  # 共通データモデル（NovelParagraph, PlaybackState, SiteType等）
│   └── common/                 # 共通ユーティリティ（TextChunker, RubyFilter, Logger等）
├── feature/
│   ├── reader/                 # 小説閲覧・WebView制御・タップ連携
│   │   ├── ui/                 # ReaderScreen, TopBar, ControlBottomBar, WebViewContainer
│   │   ├── viewmodel/          # ReaderViewModel, ReaderUiState, ReaderEvent
│   │   └── usecase/            # WebViewScriptInjectorUseCase, ProcessTappedParagraphUseCase
│   ├── playback/               # 音声読み上げ・バックグラウンド再生制御
│   │   ├── service/            # NovelPlaybackService, NotificationActionReceiver
│   │   ├── tts/                # TtsEngineManager, TtsChunkQueueManager
│   │   ├── controller/         # PlaybackController (Serviceバインダー連携)
│   │   └── usecase/            # SpeakParagraphUseCase, TogglePlaybackUseCase, ChangeSpeedUseCase
│   └── siteparser/             # サイト別パーサー（Plugin/Adapter）
│       ├── api/                # NovelSiteParser (共通IF), ParserRegistry
│       ├── kakuyomu/           # KakuyomuSiteParser (カクヨム専用DOM・ルビ除去JS)
│       ├── narou/              # NarouSiteParser (小説家になろう専用DOM・ルビ除去JS)
│       └── generic/            # GenericSiteParser (汎用Webページ用パーサー)
```
